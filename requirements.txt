# Core dependencies
pyyaml>=6.0
jinja2>=3.0
psutil>=5.0

# Scientific computing
scipy>=1.9.0
numpy>=1.21.0
pandas>=1.5.0

# Optimization framework
cvxpy>=1.3.0

# Essential CVXPY backend solvers (reliable across platforms)
clarabel>=0.5.0
scs>=3.2.0
ecos>=2.0.0
osqp>=0.6.0
cvxopt>=1.3.0

# Optional SDPA solver (specialized for semidefinite programming)
# Note: SDPA requires separate installation of SDPA binary and Python bindings
# Install with: pip install sdpa-python (if available) or compile from source
# See: https://sourceforge.net/projects/sdpa/ for official SDPA solver
sdpa-python>=0.2.0  # Uncomment if SDPA bindings are available