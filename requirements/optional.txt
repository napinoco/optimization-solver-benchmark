# Optional solver packages - platform-specific dependencies
# These may fail to install on some platforms (Windows, etc.)
# Install with: pip install -r requirements/optional.txt || echo "Optional packages failed"

# QP specialist solvers
qsqp>=0.1.0  # May not be available on all platforms

# LP-specialized solvers 
cylp>=0.91.0  # CBC interface - requires compilation
ortools>=9.4.0  # Google OR-Tools - large package with platform dependencies
highspy>=1.5.0  # HiGHS interface - compilation issues on some platforms

# Commercial solver interfaces (require licenses)
# gurobi  # Gurobi interface
# mosek   # MOSEK interface
# cplex   # CPLEX interface